pipeline {
    agent any 

    stages {
        stage('Build Stage') {
            steps {
                echo 'We are building now..'
                sh 'mvn clean install'
            }
        }

        stage('Testing Stage') {
            steps {
                echo 'Testing...'
                sh 'mvn test' 
            }
        }
    }
}